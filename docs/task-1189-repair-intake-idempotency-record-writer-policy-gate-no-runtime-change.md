# Task1189 - Repair Intake Idempotency Record Writer Policy Gate / No Runtime Change

## Status

Completed locally. Not staged.

This docs-only gate records the policy questions that must be accepted before any future Repair Intake idempotency record writer implementation.

It does not implement writer behavior.

It does not modify source/runtime files, tests, migration SQL files, package files, routes, APIs, providers, admin files, AI, billing, repository writers, or DB behavior.

It does not perform git staging, commit, cleanup, revert, reset, stash, DB connection, SQL execution, migration dry-run, migration apply, `psql`, or `db:migrate`.

## Accepted Baseline

- Task1183 committed the read-only idempotency repository.
- Task1187 committed the idempotency repository contract.
- Task1188 checkpoint is accepted.
- Current `src/repairIntake/repairIntakeIdempotencyRepository.js` keeps `recordDraftToCaseResult` unsupported and fail-closed.
- Migration 026 exists and defines `repair_intake_idempotency_records`, but it has not been executed.

## Writer Method Under Consideration

Future method:

`recordDraftToCaseResult(input)`

Intended table:

`repair_intake_idempotency_records`

Intended operation:

- Record a replay-safe draft-to-Case submit result.
- Preserve safe replay behavior for repeated submit attempts.
- Preserve organization, tenant, operation, and idempotency key scope.
- Avoid writing raw request bodies, credentials, or sensitive customer identity data.

## Required Policy Decisions Before Implementation

- `INSERT` vs `UPSERT` behavior.
- Unique conflict behavior for `organization_id`, nullable `tenant_id`, `operation_type`, and `idempotency_key`.
- Stored replay result shape and the exact allow-list for `replay_result_safe`.
- Retention and expiration behavior for `expires_at` and `retention_until`.
- Transaction boundary with Case creation and audit writing.
- Whether the idempotency record is written before or after Case creation.
- Failure behavior if idempotency record write fails after Case creation succeeds.
- Whether writer implementation must require an external transaction client injection.
- Whether failed/in-progress records should be replayable, retryable, or require manual review.
- Whether `safe_request_fingerprint` is generated by the caller or by the repository boundary.

## Safety Rules For Future Writer

- Use injected `dbClient` only.
- Use parameterized SQL only.
- Do not import a global DB client.
- Do not read `process.env` or `DATABASE_URL`.
- Do not store raw request bodies.
- Do not store credentials, tokens, cookies, authorization headers, or secrets.
- Do not store phone, address, customer name, customer phone, or customer PII.
- Do not store LINE global identity.
- Do not store `finalAppointmentId`.
- Do not expose raw SQL, SQL params, raw rows, raw DB errors, or stack traces.
- Preserve sanitized contract envelopes for recorded, replay-ready, no-existing, and failure results.
- Keep repository writer behavior isolated from route/API/provider/admin/AI/billing concerns.

## Recommended Implementation Strategy

Prefer deferring writer implementation until transaction boundary and failure semantics are accepted.

If PM chooses to proceed, first implement `recordDraftToCaseResult` as an isolated synthetic-`dbClient` unit-tested method only.

The first writer implementation should still avoid real DB execution, migration dry-run/apply, route wiring, app mount changes, and provider/admin/AI/billing/package changes.

Do not wire writer behavior into the application flow until transaction policy, conflict semantics, retention behavior, and failure handling are accepted.

## Future Bounded Task Candidates

- Writer policy decision matrix.
- Idempotency repository writer implementation with synthetic `dbClient` only.
- Submit transaction boundary design.
- Failure semantics and manual-review policy for post-Case-creation record write failure.
- Disposable DB dry-run for migration 026 if target proof is provided.

## Fail-Closed Rule

Generic `continue runtime` is not enough to implement writer behavior.

No DB execution is authorized by this document.

No writer method source change is authorized without an exact future bounded task.

`recordDraftToCaseResult` must remain unsupported/fail-closed until PM assigns and accepts a writer implementation task.

## Local Git Warning

`git diff --cached --name-only` must remain empty.

Task1189 remains untracked and unstaged.

Unrelated dirty and untracked files remain untouched.
