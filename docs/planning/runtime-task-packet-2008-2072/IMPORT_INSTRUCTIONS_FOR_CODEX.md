# Import Instructions for Codex

Import this directory into the repository at:

`docs/planning/runtime-task-packet-2008-2072/`

This import is planning-only. Do not execute Task2008–Task2072. Do not modify runtime source, deploy, smoke, DB/migration/seed, provider, billing, or AI. Do not print secrets. Do not touch the 7 held historical untracked docs.

After import, run `git status --short`, `git diff --check`, and the available project check or package-equivalent syntax/static check if npm is unavailable. Commit only the planning packet files.

Suggested commit message: `Task2008P add grouped runtime execution roadmap`
